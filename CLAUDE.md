# 딸깍 — 작업 규칙

이 파일은 세션마다 자동으로 읽힌다. `promo/` 문서는 열기로 마음먹어야 읽히니,
**기억에 의존해서 반복적으로 틀린 것만** 여기 적는다.

## ★ 아침 첫 일 — 날짜 인사를 받으면 어제 숫자부터

사용자가 **「8월 25일 화요일이야」처럼 날짜만 말하면, 어제 하루 숫자를 보고하라는 뜻이다.**
묻지 말고 딴 얘기 꺼내지 말고 바로 이것부터 돌린다.

```
set GOOGLE_APPLICATION_CREDENTIALS=C:\Users\DEV_BASIC\.secret\mineral-trainer-504400-k3-7607b66917c5.json
python scripts/ga-yesterday.py
```

키 파일은 **경로만** 쓴다. 열지도 말고 내용을 출력하지도 마라.

보고에 반드시 들어갈 것 — 사용자가 직접 열거한 항목이다:

- 어제 **몇 명 접속**했나 (세션)
- **몇 명 방문**했나 (방문자·신규·재방문)
- **누가 문제를 풀었나** (10문제 시작·완주·풀이 제출·무한 연습)
- **누가 알림을 설정했나** (알림 제안·켜기·설치)
- **몇 명이 드나들었나** (유입 출처별)

`ga-daily.py 14`는 추세를 보는 물건이다. **아침 기본은 하루짜리**고, 추세는 물어볼 때만 덧붙인다.
2026-08-25에 누적표만 내밀고 Search Console 조사로 새 버려서 지적을 받았다.

## ★ 확인할 수 있는 것은 확인하고 말한다

**원인을 지목하거나 방향을 바꾸자고 제안하기 전에, 근거를 도구로 확인한다.**
확인할 수 없으면 「모른다」 또는 「~로 보인다, 확인하려면 X」라고 말한다. 단정하지 않는다.

2026-08-24~26 사이에 여섯 번 단정하고 여섯 번 뒤집었다. 전부 5분 안에 확인 가능했다.

| 단정한 것 | 실제 | 있었던 확인 방법 |
|---|---|---|
| Bot Fight Mode가 구글 유입을 끊었다 | 8/21 크롤링 성공 | Search Console URL 검사 |
| 인앱이 리퍼러를 안 붙여 (not set)이 된다 | 채널 18개 다 잡힘 | GA 출처별 분해 |
| 쇼츠가 유입을 안 만든다 | 쇼츠 링크는 클릭 자체가 불가 | **그 파일 주석에 이미 있었다** |
| 시작률이 떨어졌다 | n=19, 기준선과 구분 불가 | 표본 크기 계산 |
| 블로그에 UTM이 빠졌다 | 들어 있었다 | 유니코드 디코드 |
| 마지막 스레드는 8/20 | 8/21 | 사용자에게 묻기 |

번복이 쌓이면 맞는 말도 안 믿긴다. 사용자가 「니 말을 믿을 수 있겠냐」고 했다.

**말하기 전에 자문한다**

1. 이걸 지금 도구로 확인할 수 있나? → 있으면 **확인하고 나서** 말한다.
2. 코드·설정 얘기인가? → **그 파일을 열어 주석부터** 읽는다. 과거의 내가 답을 적어 뒀을 수 있다.
3. 숫자로 판단하는가? → **표본 크기를 먼저 말한다**(n=19면 그렇게 적는다).
4. 기록이 비어 있는가? → 「없다」가 아니라 **「모른다」**다. 물어본다.

## 요청받은 것만 한다

다음에 뭘 해야 하는지는 **말해 주되, 실행은 요청받고** 한다. 2026-08-25에 지표 분석만
요청받고서 스레드 카드를 만들고 렌더러를 고치고 PR까지 머지했다. 앞서가면 원하지 않는
산출물이 쌓이고 정작 요청한 것이 뒷전이 된다.

한 가지를 끝내면 **결과 + 다음에 할 일 한 줄**로 멈춘다. 질문으로 끝내지도 말고
("어느 쪽 하시겠습니까?"), 실행으로 넘어가지도 마라.

## 홍보물 올리기 — 스크립트 먼저

```
node scripts/thread-post.mjs      # 스레드: 상태·공개할 정답·후보·본문 형식
```

`promo/게시-기록.md`를 손으로 읽고 판단하지 마라. 2026-08-24에 세 개를 한꺼번에
틀렸다 — 없는 행을 "안 올렸다"로 단정했고, 널린 넌센스를 문제로 골랐고, 이미지
이전 시기 형식을 끌어와 문제를 본문에 또 적었다.

- **기록이 비었으면 "안 올렸다"가 아니라 "모른다"다.** 사용자에게 물어라.
- **문제는 카드 이미지에만.** 본문에 문제를 다시 적지 않는다(8/06부터).
  `promo/스레드-첫주.md`는 폐기된 형식이다.
- **본문에 핸들을 태그하지 않는다.** 개인 반응은 각 댓글의 답글로.
- **답글에 정답을 쓰지 않고 판정도 하지 않는다.** 공개는 다음 글 본문에서만.
- **주말엔 아무것도 안 올린다.** 금요일 문제 정답은 월요일 공개.
- 자세한 형식·문제 고르는 기준: `promo/스레드-본문-형식.md`

## 오늘 출제 중인 문제는 낮에 건드리지 않는다

`src/lib/todaySafety.test.ts`가 막는다. 아침에 풀던 사람의 문제가 오후에 달라지면
완주율이 무너진다(8/11에 15%까지 떨어졌다).

**단, 표시가 깨진 것은 표시로 고친다.** 글자가 잘려서 못 푸는 것도 사고다. 문제
내용을 바꾸지 않고 컴포넌트에서 해결하면 가드도 지키고 버그도 고친다 — 8/24에
`ExampleList`가 넘치는 블록만 배율을 줄이도록 한 것이 그 예다(#258).

## 문제를 추가하면 bankHistory를 함께 고친다

`src/lib/bankHistory.ts`의 `fromDay`는 **출제 시작일(= 내일)** 이다. 배포일이 아니다.
은행이 자라면 순열이 통째로 재편되어 오늘 세트와 지난 아카이브가 소급 변경된다.

## 검증

```
npx vitest run       # 27개 파일 — todaySafety·goldenSets가 위 사고를 막는다
npm run build
```

배포는 main 머지로 자동(Cloudflare Pages). `gh` CLI는 설치되어 있지 않다 —
PR은 GitHub REST API로 만든다(자격증명은 git credential helper에 있다).

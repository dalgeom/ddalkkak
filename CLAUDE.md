# 딸깍 — 작업 규칙

이 파일은 세션마다 자동으로 읽힌다. `promo/` 문서는 열기로 마음먹어야 읽히니,
**기억에 의존해서 반복적으로 틀린 것만** 여기 적는다.

## ★ 매일 하는 일 셋 — 요청이 없어도 한다

아래 **세 가지는 「요청받은 것만 한다」의 예외다.** 요청이 없어도 하루에 한 번 한다.
빠뜨렸으면 그날 안에 따라잡고, 못 하면 **왜 못 했는지 말한다.**

| | 언제 | 무엇 |
|---|---|---|
| 1 | 날짜 인사를 받으면 바로 | **어제 GA 숫자 보고** (아래 절) |
| 2 | 사이트 수정이 끝나면 | **발견형 문제 3개 추가** (아래 절) |
| 3 | 문제 추가가 끝나면 | **홍보물** — 스레드 · 쇼츠 · 블로그 (아래 절) |

순서가 곧 우선순위다. 사이트에 고칠 것이 있으면 그것이 셋보다 먼저다.

2026-09-02·03·04 사흘 동안 2번을 통째로 빠뜨렸다. 검수·SEO·홍보만 하고
「다음: 블로그 글」로 멈췄다. 원인은 이 파일에 문제 추가가 **조건문으로만**
있었기 때문이다 — 「추가하면 bankHistory를 고쳐라」는 추가하라는 말이 아니다.
매일 하는 일은 여기 표에 있어야 시작된다.

---

## ★ 아침 첫 일 — 날짜 인사를 받으면 어제 숫자부터

사용자가 **「8월 25일 화요일이야」처럼 날짜만 말하면, 어제 하루 숫자를 보고하라는 뜻이다.**
묻지 말고 딴 얘기 꺼내지 말고 바로 이것부터 돌린다.

```
set GOOGLE_APPLICATION_CREDENTIALS=C:\Users\DEV_BASIC\.secret\mineral-trainer-504400-k3-7607b66917c5.json
python scripts/ga-yesterday.py
```

키 파일은 **경로만** 쓴다. 열지도 말고 내용을 출력하지도 마라.

보고에 반드시 들어갈 것 — 사용자가 직접 열거한 항목이다:

- 어제 **몇 명 접속**했나 (세션)
- **몇 명 방문**했나 (방문자·신규·재방문)
- **누가 문제를 풀었나** (10문제 시작·완주·풀이 제출·무한 연습)
- **누가 알림을 설정했나** (알림 제안·켜기·설치)
- **몇 명이 드나들었나** (유입 출처별)

`ga-daily.py 14`는 추세를 보는 물건이다. **아침 기본은 하루짜리**고, 추세는 물어볼 때만 덧붙인다.
2026-08-25에 누적표만 내밀고 Search Console 조사로 새 버려서 지적을 받았다.

## ★ 확인할 수 있는 것은 확인하고 말한다

**원인을 지목하거나 방향을 바꾸자고 제안하기 전에, 근거를 도구로 확인한다.**
확인할 수 없으면 「모른다」 또는 「~로 보인다, 확인하려면 X」라고 말한다. 단정하지 않는다.

2026-08-24~26 사이에 여섯 번 단정하고 여섯 번 뒤집었다. 전부 5분 안에 확인 가능했다.

| 단정한 것 | 실제 | 있었던 확인 방법 |
|---|---|---|
| ~~Bot Fight Mode가 구글 유입을 끊었다~~ | **이건 뒤집은 게 잘못이었다 — 아래 참고** | |
| 인앱이 리퍼러를 안 붙여 (not set)이 된다 | 채널 18개 다 잡힘 | GA 출처별 분해 |
| 쇼츠가 유입을 안 만든다 | 쇼츠 링크는 클릭 자체가 불가 | **그 파일 주석에 이미 있었다** |
| 시작률이 떨어졌다 | n=19, 기준선과 구분 불가 | 표본 크기 계산 |
| 블로그에 UTM이 빠졌다 | 들어 있었다 | 유니코드 디코드 |
| 마지막 스레드는 8/20 | 8/21 | 사용자에게 묻기 |

9/7에 세 번 더 했다. 앞의 여섯과 종류가 다르다 — **도구를 돌리고도 틀렸다.**

| 단정한 것 | 실제 | 무엇을 안 했나 |
|---|---|---|
| /trivia/* 24쪽에 데일리 CTA가 없다 | 밴드·하단 버튼 둘 다 있었다 | 그 파일을 안 열었다. DailyBand.svelte 주석에 「#243이 같은 이유로 /trivia/[slug]에 넣었던 것」이라고 적혀 있었다 |
| 청크에 cta_ 문자열이 없으니 계측이 안 실렸다 | 실려 있었다 | 이벤트 이름이 템플릿 리터럴(`cta_${slot}_seen`)이라 통짜로는 안 잡힌다. **패턴이 맞는지 안 봤다** |
| /discover에서 노출 계측이 안 된다 | 계측은 정상 | 내 헤드리스 스크립트가 scroll 이벤트를 하나도 안 냈다. **시험 도구를 안 재 봤다** |

**첫 줄은 2026-09-07에 되돌렸다.** 「8/21에 크롤링이 성공했다」는 노출이 0인 것을
반증하지 못한다 — 크롤링과 노출은 다른 단계다. 그때 URL 검사만 보고 뒤집었는데,
GSC **일별 노출**을 그날 아무도 안 봤다. 뽑아 보니 이렇다.

    8/15  64 · 8/16  90 · 8/17  73 · 8/18  76 · 8/19  34
    8/20   1  ← Bot Fight Mode 켠 날
    8/21~26  0 · 1 · 0 · 0 · 0 · 0      (8/24에 껐는데도 이틀 더 0)
    8/27   3 · 8/28  9 · 8/29 12 · 9/02 18   ← 사흘 뒤부터 돌아온다

크롤링 통계는 같은 기간 내내 멀쩡했다(200이 98%, 호스트 「문제 없음」, 8/20~26에
요청 급감 없음). **크롤링은 되는데 노출만 0이었다.** promo/지표-기록.md:65도 GA로
같은 것을 봤다(google organic 41 → 0).

그날 배포가 여러 건이라 인과를 단정하지는 않는다. 다만 **「틀린 단정」으로 분류해
둔 것이 틀렸다.** 이 표의 목적은 「확인하고 말하라」인데, 확인의 대상을 잘못 고른
사례를 반증으로 남겨 두면 다음에 같은 자리를 또 잘못 짚는다.

번복이 쌓이면 맞는 말도 안 믿긴다. 사용자가 「니 말을 믿을 수 있겠냐」고 했다.
**뒤집는 것도 근거가 있어야 한다** — 뒤집기를 잘하는 것이 목적이 아니다.

**말하기 전에 자문한다**

1. 이걸 지금 도구로 확인할 수 있나? → 있으면 **확인하고 나서** 말한다.
2. **어떤 파일을 고치자고 말하려는가? → 그 파일을 먼저 연다.** 주석부터 읽는다.
   코드를 건드릴 때만이 아니다. **지표 분석·제안·다음 할 일 한 줄에서도** 특정
   파일이나 페이지를 지목하는 순간 이 항목이 걸린다 — 9/7에 「CTA가 없다」고 한 것이
   코드 작업이 아니라 GA 보고였기 때문에 이 항목을 그냥 지나쳤다.
3. 숫자로 판단하는가? → **표본 크기를 먼저 말한다**(n=19면 그렇게 적는다).
4. 기록이 비어 있는가? → 「없다」가 아니라 **「모른다」**다. 물어본다.
5. **「0건·안 나옴·안 찍힘」을 증거로 쓰려는가? → 그 도구가 양성을 잡는지 먼저 보인다.**
   음성 결과는 대상이 깨끗하다는 뜻일 수도, 도구가 죽었다는 뜻일 수도 있다. 둘은
   화면에서 똑같이 생겼다. 확인 방법은 늘 같다 — **있는 줄 아는 것 하나를 잡아 본다.**

   | 상황 | 양성 대조 |
   |---|---|
   | grep이 0건 | 반드시 있는 문자열로 같은 패턴을 한 번 돌려 본다 |
   | 계측이 안 찍힌다 | 손으로 이벤트를 한 번 일으켜 본다 |
   | 테스트가 통과했다 | 일부러 깨뜨려 실제로 빨개지는지 본다 |
   | 헤드리스가 아무 반응 없다 | 그 조작이 진짜 일어났는지(스크롤 수·좌표) 센다 |

   이 프로젝트는 같은 것에 세 번 데였다. `todaySafety`·`bankIntegrity`가 fetch-depth
   때문에 CI에서 늘 빈손으로 통과했고(9/3), `ruleAttack.test.ts`에 「검사기가 실제로
   문제를 읽고 있다」는 세 번째 테스트를 그래서 넣었고(9/3), 9/7에 또 두 번 했다.

## 요청받은 것만 한다

다음에 뭘 해야 하는지는 **말해 주되, 실행은 요청받고** 한다. 2026-08-25에 지표 분석만
요청받고서 스레드 카드를 만들고 렌더러를 고치고 PR까지 머지했다. 앞서가면 원하지 않는
산출물이 쌓이고 정작 요청한 것이 뒷전이 된다.

한 가지를 끝내면 **결과 + 다음에 할 일 한 줄**로 멈춘다. 질문으로 끝내지도 말고
("어느 쪽 하시겠습니까?"), 실행으로 넘어가지도 마라.

**단, 맨 위의 「매일 하는 일 셋」은 예외다.** 그 셋은 요청을 기다리지 않는다.
이 절 때문에 9/2~9/4 사흘간 문제 추가가 통째로 빠졌다 — 적혀 있지 않은 루틴은
「요청받은 것만」에 걸려 영원히 시작되지 않는다.

## 홍보물 올리기 — 스크립트 먼저

```
node scripts/thread-post.mjs      # 스레드: 상태·공개할 정답·후보·본문 형식
```

`promo/게시-기록.md`를 손으로 읽고 판단하지 마라. 2026-08-24에 세 개를 한꺼번에
틀렸다 — 없는 행을 "안 올렸다"로 단정했고, 널린 넌센스를 문제로 골랐고, 이미지
이전 시기 형식을 끌어와 문제를 본문에 또 적었다.

- **기록이 비었으면 "안 올렸다"가 아니라 "모른다"다.** 사용자에게 물어라.
- **문제는 카드 이미지에만.** 본문에 문제를 다시 적지 않는다(8/06부터).
  `promo/스레드-첫주.md`는 폐기된 형식이다.
- **본문에 핸들을 태그하지 않는다.** 개인 반응은 각 댓글의 답글로.
- **답글에 정답을 쓰지 않고 판정도 하지 않는다.** 공개는 다음 글 본문에서만.
- **주말엔 아무것도 안 올린다.** 금요일 문제 정답은 월요일 공개.
- 자세한 형식·문제 고르는 기준: `promo/스레드-본문-형식.md`

## 「올렸어」를 들으면 기록하고 파일을 지운다

사용자가 게시를 확인하면 **둘 다** 한다. 디스크에 매일 쌓이는 게 싫다는 요청이다(9/8).

1. `promo/게시-기록.md`에 행을 추가한다 — 「사용자가 올렸다고 확인한 것만 적는다」
2. `node scripts/promo-clean.mjs --지워` — 기록에 있는 산출물만 지운다

**기록이 먼저다.** 순서가 바뀌면 방금 그 파일이 「기록에 없는 것」으로 분류돼 안 지워진다.

**폴더를 통째로 지우지 마라.** promo/ 안에는 성격이 다른 둘이 섞여 있다.

| | 지운다 | 지키다 |
|---|---|---|
| social | 스레드-날짜.png | — |
| video | 쇼츠-*.mp4 | — |
| naver | 글 본문 이미지(함정-·제작-·성냥- 등) | 프로필.png · 커버.png |
| youtube | — | 프로필·배너·워터마크.png |
| images | — | 제품 스크린샷 전체 |

지키는 쪽은 `채널설정.md`·`블로그정보.md`·블로그 원고가 계속 참조한다. 「promo 전부」로
지우면 유튜브 채널 배너와 블로그 커버가 날아간다. 스크립트의 KEEP이 그걸 막으니
**`rm`을 직접 쓰지 말고 스크립트를 써라.** 인자 없이 돌리면 지울 목록만 보여준다.

## 오늘 출제 중인 문제는 낮에 건드리지 않는다

`src/lib/todaySafety.test.ts`가 막는다. 아침에 풀던 사람의 문제가 오후에 달라지면
완주율이 무너진다(8/11에 15%까지 떨어졌다).

**단, 표시가 깨진 것은 표시로 고친다.** 글자가 잘려서 못 푸는 것도 사고다. 문제
내용을 바꾸지 않고 컴포넌트에서 해결하면 가드도 지키고 버그도 고친다 — 8/24에
`ExampleList`가 넘치는 블록만 배율을 줄이도록 한 것이 그 예다(#258).

## 매일 발견형 문제 3개 — 만드는 법

**하루 한 번, 요청 없이 한다.** 8/24부터 9/1까지는 3개씩 붙였고 9/2~9/4에 끊겼다.
하루 4문제씩 나가고 은행이 339개라 반복까지 85일 여유지만, 리듬이 끊기면 안 붙는다.

만들 때 지키는 것

- **후보를 넉넉히 만들고 많이 버린다.** 기록이 그렇다 — 「8개 중 5개를 버렸다」
  (9/1), 「5개 중 2개」(8/31), 「검수 5차까지」(8/27). 3개를 뽑으려면 6~8개를 만든다.
- **검수는 `.claude/agents/puzzle-reviewer.md`의 게이트 전부를 통과해야 한다.**
  발견형은 5a~5g(기계 공격·약화·강화·경계 덮개·사실 확인·해설 검산·답 불변),
  계산형은 5h~5l(정답 키 기계 검산·경계 약속 표·선택지 집합·입력 정규화·화면 대조).
- **기계 공격을 돌린다** — `npx vite-node scripts/rule-attack.mjs`. 적발 0이어야 한다.
  `ruleAttack.test.ts`의 알려진_적발 목록은 비어 있고, 하나라도 걸리면 CI가 깨진다.
- 중복은 **소재가 아니라 메커니즘**으로 본다. chip 개수 과밀도 함께 본다.

## 문제를 추가하면 bankHistory를 함께 고친다

`src/lib/bankHistory.ts`의 `fromDay`는 **출제 시작일(= 내일)** 이다. 배포일이 아니다.
은행이 자라면 순열이 통째로 재편되어 오늘 세트와 지난 아카이브가 소급 변경된다.

골든 픽스처도 함께 늘린다 — `GOLDEN_APPEND=1 npx vitest run src/lib/goldenSets.test.ts`.

## 검증

```
npx vitest run       # 27개 파일 — todaySafety·goldenSets가 위 사고를 막는다
npm run build
```

배포는 main 머지로 자동(Cloudflare Pages). `gh` CLI는 설치되어 있지 않다 —
PR은 GitHub REST API로 만든다(자격증명은 git credential helper에 있다).
